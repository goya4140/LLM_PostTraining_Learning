# ============================================================
# LLM_PostTraining_Learning — 云端环境（A100/H100，CUDA 12.1）
# Python 3.11
#
# 安装步骤：
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install torch==2.5.1 torchvision==0.20.1 \
#       --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_cloud.txt
#   pip install flash-attn --no-build-isolation  # 可选，需编译
# ============================================================

# HuggingFace 生态（PyTorch 通过上方命令单独安装）
transformers>=4.46.0,<5.0
datasets>=3.0.0,<4.0
tokenizers>=0.20.0
acceleate>=1.1.0,<2.0
peft>=0.13.0,<1.0
trl>=0.12.0,<1.0

# 量化（CUDA 完整支持）
bitsandbytes>=0.44.0

# 分布式训练（多卡，可选）
deepspeed>=0.15.0

# 数据处理
numpy>=1.26.0,<2.0
pandas>=2.0.0
scipy>=1.11.0

# 代码执行沙箱（05_GRPO code reward 需要）
docker>=7.0.0

# 可视化与日志
matplotlib>=3.8.0,<4.0
tqdm>=4.66.0
tensorboard>=2.15.0
wandb>=0.18.0

# 工具
huggingface_hub>=0.25.0
sentencepiece>=0.2.0
protobuf>=4.25.0
evaluate>=0.4.0
